#include <stdio.h>

int main (int argc, char *argv[]){
    // Variables
    // Status struct, file, memory?

    // Read file input (file, memory)

    // Process instructions (status, memory)

    return 0;
}
